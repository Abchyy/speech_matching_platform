# Agent 协作规则

本文件只记录长期稳定规则。产品范围、技术方案与当前任务细节分别见 `docs/` 与 `PROGRESS.md`。

## 设计资产

- `docs/` 中经人工确认的设计文档是项目基线，不得擅自修改、重写或重新生成。
- 未冻结的产品决策必须由人工确认，不得由 Agent 自行决定或写入代码。
- 不得自行扩展产品范围，也不得用实现细节覆盖已确认的产品定位。

## Git 协作

- 使用清晰、可审查的提交信息；一次提交只做一类变更。
- 不提交密钥、本地环境文件、生成物和无关产物。
- 不以 force push 覆盖共享分支的历史，除非人工明确要求。
- 推送、合并与发布前确认当前分支状态，避免覆盖他人工作。

## 代码质量

- 保持代码可读、可维护，避免无关重构和范围外改动。
- 只实现当前阶段需要的内容，不为未确认需求提前设计。
- 修改应可追溯、可回滚，并与已确认文档保持一致。

## 多人 / 多 Agent 协作

- 先读 `PROGRESS.md` 和相关文档，再开始改动。
- 不并行覆盖同一文件的未协调修改。
- 发现可能改变产品方向的问题，先指出并等待人工确认，不要自行决策。
